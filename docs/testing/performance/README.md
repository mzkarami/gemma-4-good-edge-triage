# Performance Evidence

Status: performance evidence index.

Use `../../CURRENT_FRONTIER.md` for current public benchmark claims. Use `../../superpowers/research_logs/` for EDG experiment history until research logs are migrated under `docs/research/`.

Performance docs should include:

- profile name;
- model artifact;
- sample count;
- F1/accuracy/latency;
- GPU/CPU comparability status;
- run ID and state hash where available;
- promotion decision.
