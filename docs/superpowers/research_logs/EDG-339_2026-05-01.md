# EDG-339 - Keyword Boundary Hot-Path Trim (2026-05-01)

## Objective
- Continue EDG-338 follow-up: reduce low-level routing/accounting overhead without changing the promoted prompt semantics.
- Preserve current promoted runtime knobs:
  - `N_CTX=933`
  - `N_GPU_LAYERS=47`
  - `LLAMA_N_BATCH=192`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=11`

## Change
- Replaced regex-based boundary checks in keyword matching (`\bterm\b` via `re.search`) with low-overhead space-delimited boundary matching for single-token terms.
- Kept phrase and underscore keyword behavior as substring checks.
- Updated experiment signature to force a fresh measurable state hash for this code-path-only change.

## Runs
- `EDG-339 r1`
  - F1: `0.9794`
  - Latency: `160.59 ms`
  - TTFT: `155.73 ms`
  - Peak VRAM: `3.84 GB`
- `EDG-339 r2`
  - F1: `0.9794`
  - Latency: `161.75 ms`
  - TTFT: `156.80 ms`
  - Peak VRAM: `3.84 GB`
- `EDG-339 r3`
  - F1: `0.9794`
  - Latency: `162.23 ms`
  - TTFT: `157.41 ms`
  - Peak VRAM: `3.84 GB`
- Shared state hash across all repeats:
  - `75a9c4e333ff2c6825a91b5d05d0c07bb094c06df1c591d3e95cadf0e0ec5267`

## Stability Summary
- F1 is fully stable across repeats.
- Latency band is tight (`160.59-162.23 ms`, spread `1.64 ms`).
- Routing mix stayed constant (`metadata_shortcut=23`, `targeted_probe=19`, `full_mm=13`, `shortcut_ratio=0.84`).

## Decision
- `keep` (all three runs recorded as keep by the sandbox gate).
- Rationale:
  - Maintains frontier F1 with stable repeatability.
  - Consistently better than EDG-338 r7 (`166.59 ms`) and competitive with current kept frontier under epsilon policy.
  - Change remains low-risk and local to keyword matching internals.

## Next Step
- Explore one additional low-risk overhead trim in the same routing layer (e.g., avoiding repeated string normalization on metadata-empty paths) and run an A/B repeat pair against EDG-339.
