# EDG-384 — Latency-Outlier Ratio Calibration (16-sample comparable sweep)

Date: 2026-05-08
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so execution followed the open next action from `EDG-383`.

## Objective
Calibrate `TRIAGE_LATENCY_OUTLIER_RATIO` using a 16-sample comparable sweep to verify the EDG-383 guard does not create false-positive blocked runs under stable GPU conditions.

Grid tested:
- `TRIAGE_LATENCY_OUTLIER_RATIO=1.30`
- `TRIAGE_LATENCY_OUTLIER_RATIO=1.40`
- `TRIAGE_LATENCY_OUTLIER_RATIO=1.50`

Shared run controls:
- `TRIAGE_FORCE_RERUN=1`
- `TRIAGE_EVAL_MAX_SAMPLES=16`
- default routing bundle (`metadata_shortcuts=1`, `targeted_probes=1`, probe budget default 11)

## Executed
```bash
for ratio in 1.30 1.40 1.50; do
  TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
  TRIAGE_LATENCY_OUTLIER_RATIO=${ratio} \
  uv run triage_sandbox.py \
    --description "[latv2] EDG-384 r${ratio}: 16-sample latency-outlier calibration ratio=${ratio}"
done
```

## Results Snapshot
Persisted rows in `results.tsv`:

1. `run_id=20260508T140055Z` (`ratio=1.30`)
- `f1_score=0.9458`
- `latency_ms=204.93`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard` (historical best gate)

2. `run_id=20260508T140102Z` (`ratio=1.40`)
- `f1_score=0.9458`
- `latency_ms=219.70`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard` (historical best gate)

3. `run_id=20260508T140109Z` (`ratio=1.50`)
- `f1_score=0.9458`
- `latency_ms=208.07`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard` (historical best gate)

## Interpretation
- No run was marked `blocked` by the latency-outlier guard in this stable-GPU comparable slice.
- All three ratios preserved normal evaluation flow and produced identical quality outcomes (`F1=0.9458`), with latency variation inside expected noise.
- This supports the EDG-383 claim that outlier blocking is selective rather than aggressively triggering on healthy runs.

## Recommendation
- Keep default `LATENCY_OUTLIER_RATIO=1.45` unchanged for now:
  - `1.30` appears potentially too tight for broader noisy environments.
  - `1.50` is safer but weaker against genuine outliers.
  - `1.45` remains a balanced midpoint and matches current implementation default.

## Next Action
1. Run one stress check that intentionally induces a same-hash high-latency condition (e.g., temporary reduced GPU layers) and verify exactly one `blocked: non-comparable latency outlier` is recorded with duplicate blocked suppression on rerun.
2. If validated, mark EDG-383/384 guard rollout as complete and move optimization focus back to F1 recovery on 16-sample comparable runs.
