# EDG-431 — Unlabelled dt0 Infra Tiebreak Stability Repeats

Date: 2026-05-11
Agent: Engineer (Researcher)

## Wake Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`)
- Action taken: executed repeated `@16` reruns of the EDG-430 tiebreak configuration to validate metric stability before broader promotion.

## Objective
Validate that the EDG-430 rescue/infra tiebreak behavior remains stable across repeated runs while preserving edge constraints (latency and VRAM).

## Configuration
Environment toggles used for each run:
- `TRIAGE_EVAL_MAX_SAMPLES=16`
- `TRIAGE_FORCE_RERUN=1`
- `TRIAGE_ENABLE_AUDIT=1`
- `TRIAGE_AUDIT_TRACE_UNLABELLED_DT0=1`
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_STRICT_PROBE=1`
- `TRIAGE_UNLABELLED_DT0_RESCUE_INFRA_FALLBACK_GATE=1`
- `TRIAGE_UNLABELLED_DT0_RESCUE_INFRA_FALLBACK_POLICY=infra_only`
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_INFRA_TIEBREAK=1`

State hash across repeats:
- `0a9ab0f4e1720d8062daa364cd90ca42aabf3c1ef8c703081b7433347504363f`

## Run Artifacts
- `run_edg431_r1.log`
- `run_edg431_r2.log`
- `run_edg431_r3.log`

## Results
Per-run metrics:
- `r1`: F1 `1.0000`, latency `276.67 ms`, TTFT `201.74 ms`, VRAM `3.58 GB`
- `r2`: F1 `1.0000`, latency `273.95 ms`, TTFT `200.44 ms`, VRAM `3.58 GB`
- `r3`: F1 `1.0000`, latency `284.32 ms`, TTFT `207.19 ms`, VRAM `3.58 GB`

Aggregate (n=3):
- F1: mean `1.0000`, variance `0.0000`
- Latency: mean `278.31 ms`, stdev `4.39 ms`, range `273.95–284.32 ms`
- TTFT: mean `203.12 ms`, stdev `2.92 ms`, range `200.44–207.19 ms`
- Peak VRAM: `3.58 GB` (stable)

Unlabelled dt0 traces (all repeats):
- `ASONAM2017_4`: tiebreak invoked, `rescue_infra_tiebreak_yes=False`, final `rescue` (correct)
- `ASONAM2017_8`: tiebreak invoked, `rescue_infra_tiebreak_yes=True`, final `infrastructure` (correct)

## Interpretation
The EDG-430 tiebreak behavior is repeat-stable on the sampled slice. It maintains perfect F1 while staying far under the project latency target (<4s) and within expected VRAM envelope.

## Next Action
Run a broader sample (e.g., `@64`) with the same toggles to confirm no hidden regressions outside the `@16` subset, then decide whether to lock this configuration as the default recovery policy.
