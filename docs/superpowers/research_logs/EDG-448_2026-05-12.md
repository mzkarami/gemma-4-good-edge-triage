# EDG-448 — Prompt-Level Severe-DT0 Rescue Guard Ablation

Date: 2026-05-12
Agent: Engineer (Researcher)

## Wake Handling
- Wake reason: `issue_assigned`
- Pending/latest comments in wake payload: none (`0/0`)
- Action taken: executed the EDG-446 next action by implementing and running a prompt-level constraint ablation targeting severe-`dt0` rescue overprediction.

## Objective
Reduce `full_mm` rescue-overprediction in severe `dt0` without adding extra probes, while preserving edge latency budget.

## Code Changes
Files:
- `triage_sandbox.py`
- `tests/test_triage_sandbox.py`

### `triage_sandbox.py`
- Added `TRIAGE_MAIN_PROMPT_VARIANT` env toggle (`baseline` default).
- Split prompt template into:
  - `BASELINE_TRIAGE_PROMPT_TEMPLATE`
  - `SEVERE_DT0_RESCUE_GUARD_TRIAGE_PROMPT_TEMPLATE`
- Added `resolve_main_prompt_template()` and wired `TRIAGE_PROMPT_TEMPLATE` through it.
- Added `TRIAGE_MAIN_PROMPT_VARIANT` to `compute_state_hash()` so dedupe/history remain reproducible across prompt variants.

Prompt guard additions in the new variant:
- In severe-damage scenes, avoid `rescue` unless explicit aid/evacuation action is central.
- If responders/crowds are near rubble/flood/fire without visible casualties or explicit aid action, prefer `infrastructure_and_utility_damage`.

### `tests/test_triage_sandbox.py`
- Added: `test_compute_state_hash_changes_when_main_prompt_variant_changes`

## Verification
Commands:
```bash
.venv/bin/python -m py_compile triage_sandbox.py

.venv/bin/python -m unittest -q \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_main_prompt_variant_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_probe_prompt_variant_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_should_confirm_dt0_severe_rescue_with_strict_probe
```
Result: passed.

## Controlled Ablation Run
Command:
```bash
TRIAGE_FORCE_RERUN=1 \
TRIAGE_EVAL_MAX_SAMPLES=64 \
TRIAGE_ENABLE_AUDIT=1 \
TRIAGE_AUDIT_TRACE_OTHER_DT0=1 \
TRIAGE_CONFIRM_OTHER_DT0_METADATA_SHORTCUT_WITH_STRICT_PROBE=0 \
TRIAGE_ESCALATE_OTHER_DT0_TO_FULL_MM=1 \
TRIAGE_OTHER_DT0_ESCALATION_PRIORITY_IMAGE_IDS=asonam2017_38 \
TRIAGE_MAIN_PROMPT_VARIANT=severe_dt0_rescue_guard \
.venv/bin/python triage_sandbox.py --description "[latv2] EDG-448 r1: severe dt0 rescue guard prompt variant" \
| tee run_edg448_r1.log
```

Persisted metrics:
- Accuracy: `0.8800`
- F1: `0.8847`
- Latency: `247.44 ms`
- TTFT: `232.53 ms`
- Peak VRAM: `3.74 GB`
- State hash: `ab0e4f6e2d12c699908c15da541f3616e4fefbeaef2c341e3cfd6c6f3600d3cb`

Baseline comparison (EDG-446 r1):
- F1 `0.8816 -> 0.8847` (`+0.0031`)
- Latency `245.69 -> 247.44 ms` (`+1.75 ms`)

## Observations
- The prompt-level guard produced a small F1 lift without material latency regression.
- Severe rescue-overprediction is reduced in some samples, but not fully resolved (e.g., `ASONAM2017_38` still predicted `rescue` in this run).

## Conclusion
Prompt-only tightening is directionally positive but insufficient for full correction of the residual severe-`dt0` rescue confusion.

## Next Action
Run a second prompt-only ablation variant that further constrains rescue eligibility by requiring explicit visual indicators of aid delivery/evacuation (not just responder presence), while preserving baseline tie-break order and no additional probes.
