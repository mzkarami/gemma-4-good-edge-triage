# EDG-434 — none_dt5 Strict-Confirmation Probe for False-Negative Recovery

Date: 2026-05-11
Agent: Engineer (Researcher)

## Wake Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`)
- Action taken: implemented and benchmarked a targeted `none_dt5` recovery gate aimed at the `targeted_probe_none_dt5_no` miss seen in EDG-432.

## Objective
Recover the `ASONAM2017_20` false negative (`affected` -> `not_humanitarian`) without regressing latency/VRAM constraints.

## Change Implemented
Code path:
- `triage_sandbox.py`

New feature flag:
- `TRIAGE_CONFIRM_NONE_DT5_PROBE_NO_WITH_STRICT_PROBE` (default `0`)

Behavior:
- In metadata placeholder route `event_bucket=none` and `disaster_type=5`, when primary affected probe returns `no`, optionally run strict affected probe before finalizing `not_humanitarian`.
- If strict probe returns `yes`, route to `affected_injured_or_dead_people` with audit route `targeted_probe_none_dt5_no_strict_yes`.
- Added to `compute_state_hash()` to keep experiment dedupe/config identity correct.

Test coverage:
- Added hash regression test in `tests/test_triage_sandbox.py`:
  - `test_compute_state_hash_changes_when_none_dt5_probe_no_strict_confirmation_changes`
- Verified via:
  - `uv run python -m unittest tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_none_dt5_probe_no_strict_confirmation_changes`

## Benchmark Run (r1)
Environment toggles (baseline EDG-432 broad slice + new gate):
- `TRIAGE_EVAL_MAX_SAMPLES=64` (effective 50 available)
- `TRIAGE_FORCE_RERUN=1`
- `TRIAGE_ENABLE_AUDIT=1`
- `TRIAGE_AUDIT_TRACE_UNLABELLED_DT0=1`
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_STRICT_PROBE=1`
- `TRIAGE_UNLABELLED_DT0_RESCUE_INFRA_FALLBACK_GATE=1`
- `TRIAGE_UNLABELLED_DT0_RESCUE_INFRA_FALLBACK_POLICY=infra_only`
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_INFRA_TIEBREAK=1`
- `TRIAGE_CONFIRM_NONE_DT5_PROBE_NO_WITH_STRICT_PROBE=1`

Artifacts:
- `run_edg434_r1.log`
- `results.tsv` row at `20260511T160149Z`

Results:
- F1 `0.9374`, latency `228.91 ms`, TTFT `170.30 ms`, VRAM `3.74 GB`
- State hash: `c33568e1e0bb5764f254226e71554df70d2502da390da61bbccbaf04d4796efb`
- Ledger status: `discard` (F1 below historical best)

Mismatch audit (unchanged vs EDG-432):
- `ASONAM2017_20`: gold `affected_injured_or_dead_people` -> pred `not_humanitarian` (`targeted_probe_none_dt5_no`)
- `ASONAM2017_38`: gold `affected_injured_or_dead_people` -> pred `infrastructure_and_utility_damage` (`metadata_shortcut_other_dt0`)
- `ASONAM2017_44`: gold `not_humanitarian` -> pred `infrastructure_and_utility_damage` (`full_mm`)

## Interpretation
- The strict confirmation fallback did not trigger a correction on the known `none_dt5` miss; route remained `targeted_probe_none_dt5_no`.
- This indicates the failure is not from missing second-opinion gating, but from both probe variants agreeing on `no` for that sample.
- Latency and VRAM remain safely within edge constraints (<4s, <4 GB), but no quality gain.

## Next Action
Prioritize the second high-leverage route from EDG-432:
1. Add a narrow guard for `metadata_shortcut_other_dt0` by requiring selective full-MM confirmation on likely-human-harm cues before infra shortcut finalization.
2. Re-run broad-slice audit (`@64` effective 50) and verify whether `ASONAM2017_38` is recovered without harming `ASONAM2017_44`.
