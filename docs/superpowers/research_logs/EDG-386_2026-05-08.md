# EDG-386 — Latency-Outlier Block Lifecycle Coverage

Date: 2026-05-08
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work advanced from EDG-384 next action.

## Objective
Leave durable progress for the latency-outlier guard rollout by proving the full blocked-lifecycle behavior in deterministic test coverage:
1. First pass records `blocked: latency outlier vs same-state median`.
2. Same-hash rerun short-circuits before model load and does not append duplicate blocked rows.

## Changes Implemented
- Added unit test:
  - `tests/test_triage_sandbox.py`
  - `test_run_triage_records_latency_outlier_block_then_short_circuits_duplicate_rerun`
- Test structure:
  - First run patches `detect_latency_outlier_for_state_hash(...)` to return outlier metadata and verifies a blocked row is written.
  - Second run reuses the same state hash and simulates blocked-reason match for `latency outlier vs same-state median`, then verifies:
    - no model load occurs
    - no extra row is appended to `results.tsv`

## Verification
Executed:
```bash
uv run python -m unittest -q tests.test_triage_sandbox
```

Result:
- `Ran 50 tests ... OK`
- Confirms outlier-block dedupe lifecycle behavior is enforced and regression-protected.

## Next Action
1. Run one live stress check with intentionally induced host variance on same-hash config to validate runtime behavior outside mocks.
2. If live check matches test behavior, close the EDG-383/384 guard rollout and move back to F1/latency frontier tuning on comparable 16-sample sweeps.
