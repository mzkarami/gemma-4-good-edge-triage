# EDG-404 — Unlabelled dt0 Rescue Strict Confirmation Probe

Date: 2026-05-09
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work proceeded immediately.

## Objective
Address the remaining EDG-402 audit miss on `dt0_unlabelled` full-MM rescue-vs-infrastructure confusion by introducing a narrow strict confirmation gate for rescue predictions.

## Code Changes
Files updated:
- `triage_sandbox.py`
- `tests/test_triage_sandbox.py`

### New runtime knob (default OFF)
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_STRICT_PROBE=0|1`

### Behavior
- Added helper:
  - `should_confirm_unlabelled_dt0_rescue_with_strict_probe(...)`
- In full-MM post-processing for `dt0_unlabelled` samples:
  - When prediction is `rescue_volunteering_or_donation_effort` and targeted affected probe is negative, optional strict affected probe can now run.
  - If strict probe is negative, demote prediction to `infrastructure_and_utility_damage`.
- Added knob to `compute_state_hash()` for reproducible experiment identity.

## Verification
Focused unit checks:
```bash
uv run python -m unittest -q \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_unlabelled_dt0_rescue_strict_probe_toggle_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_should_confirm_unlabelled_dt0_rescue_with_strict_probe \
  tests.test_triage_sandbox.TestTriageSandbox.test_should_confirm_dt0_severe_affected_with_strict_probe \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_dt0_severe_strict_probe_confirmation_changes
```
Result: `Ran 4 tests ... OK`

## Experiment
Executed:
```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_ENABLE_AUDIT=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=baseline TRIAGE_ENABLE_DT0_SEVERE_PROBE=0 \
TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_STRICT_PROBE=1 \
uv run triage_sandbox.py --description "[latv2] EDG-404 r1: unlabelled dt0 rescue strict confirmation @16"
```

### Results
- `run_id=20260509T180231Z`
- `state_hash=82ae89b4fcada94a5825b358fab80b45ad996ede6fbda801dddf8ee688998a9e`
- `f1_score=0.9081`
- `latency_ms=246.96`
- `vram_gb=3.57`
- `status=discard`

Audit summary:
- `mismatches=1`
- New error profile:
  - `idx=4`, `event=ecuador_eq_unlabelled_im_828.jpg`, `gold=rescue_volunteering_or_donation_effort`, `pred=infrastructure_and_utility_damage`, `route=full_mm`

## Interpretation
- The new strict rescue confirmation removed the previous infra miss on unlabelled dt0, but it over-corrected and introduced the inverse rescue->infrastructure error.
- Net quality regressed materially vs EDG-402 control (`0.9458 -> 0.9081`) with latency increase (`221.57 -> 246.96 ms`).
- This confirms strict-casualty gating is too aggressive for unlabelled dt0 rescue scenes.

## Decision
- Keep code path behind default-OFF toggle only.
- Mark EDG-404 r1 configuration as discard.

## Next Action
1. Pivot to confidence-aware tie-break calibration on unlabelled dt0 full-MM outputs (use lexical rescue-evidence cues rather than casualty strictness).
2. Preserve severe-dt0 strict affected guard from EDG-402 as the stable baseline.
