# EDG-398 — Targeted Probe Prompt Variant A/B (none_dt6 / other_dt0)

Date: 2026-05-09
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work proceeded directly with EDG-397 follow-up prompt-quality testing.

## Objective
Execute the planned prompt-quality experiment on targeted affected-probe wording (without changing routing structure) to improve F1 on the known `none_dt6` / `other_dt0` confusion slices while preserving latency budget.

## Changes Implemented
File: `triage_sandbox.py`

Added a prompt-variant switch for targeted affected probes:
- `TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=baseline|v2|v3` (default `baseline`)

Implementation details:
- Added alternate probe prompts:
  - `TARGETED_AFFECTED_USER_PROMPT_V2`
  - `TARGETED_AFFECTED_USER_PROMPT_V3`
  - `STRICT_TARGETED_AFFECTED_USER_PROMPT_V2`
- Added resolver:
  - `resolve_targeted_probe_prompts()`
  - `ACTIVE_TARGETED_AFFECTED_USER_PROMPT`
  - `ACTIVE_STRICT_TARGETED_AFFECTED_USER_PROMPT`
- Wired targeted probes to active prompts for both standard and strict probe calls.
- Included prompt variant and active prompt text in `compute_state_hash()` to preserve reproducible experiment identity.

Validation updates in `tests/test_triage_sandbox.py`:
- Updated existing targeted-prompt hash test to patch active prompt variable.
- Added:
  - `test_compute_state_hash_changes_when_probe_prompt_variant_changes`

## Verification
### Focused unit checks
Executed:
```bash
uv run python -m unittest -q \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_targeted_probe_prompt_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_probe_prompt_variant_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_probe_budget_changes
```

Result:
- `Ran 3 tests ... OK`

### 16-sample paired A/B

Run A (control):
```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=baseline \
uv run triage_sandbox.py --description "[latv2] EDG-398 r1: baseline probe wording control @16"
```

Persisted row:
- `run_id=20260509T100112Z`
- `state_hash=f829861c89fcbedc46d60d414c495ed3977c879ed35338af243218ccea13c31e`
- `f1_score=0.9458`
- `latency_ms=220.35`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard`
- Routing mix: `metadata_shortcut=6`, `targeted_probe=8`, `full_mm=6`, `shortcut_ratio=0.88`

Run B (variant):
```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=v2 \
uv run triage_sandbox.py --description "[latv2] EDG-398 r2: v2 stricter casualty-vs-rescue probe wording @16"
```

Persisted row:
- `run_id=20260509T100121Z`
- `state_hash=7411a859d35c36077829aa5eb6a3df9d5920ccfdc7e4155d540e2dec8dfacef8`
- `f1_score=0.8494`
- `latency_ms=219.96`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard`
- Routing mix: `metadata_shortcut=6`, `targeted_probe=8`, `full_mm=6`, `shortcut_ratio=0.88`

## Interpretation
- Prompt-only tightening (`v2`) materially degraded F1 (`0.8494`) versus control (`0.9458`) with essentially unchanged latency.
- The current confusion ceiling appears not to be solved by stricter casualty wording alone under unchanged routing.
- Baseline remains the safest setting for this axis.

## Next Action
1. Keep `TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=baseline` as default for production loop continuity.
2. Run one additional paired test with `TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=v3` and `TRIAGE_ENABLE_DT0_SEVERE_PROBE=1` to test whether narrower severe-dt0 pre-filtering plus conservative wording improves F1 without increasing shortcut ratio.
3. If no recovery above `0.9458`, pivot from probe wording to route-level selective full-MM escalation only on misclassified `none_dt6` pairs captured by audit.
