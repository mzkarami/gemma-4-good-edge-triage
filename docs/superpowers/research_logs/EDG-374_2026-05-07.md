# EDG-374 - Edge-Triage Optimization Loop (2026-05-07)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so execution followed the open next action from `EDG-372` directly.

## Objective
- Run the planned third 4-sample comparable micro-check with metadata shortcuts enabled and targeted probes reintroduced at a reduced budget.
- Validate whether F1 recovers relative to `EDG-372 r1` while preserving sub-4s latency.

## Work Completed
Executed:

```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=4 \
TRIAGE_ENABLE_METADATA_SHORTCUTS=1 TRIAGE_ENABLE_TARGETED_PROBES=1 \
TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4 \
uv run triage_sandbox.py \
  --description "[latv2] EDG-374 r1: 4-sample micro-check metadata shortcuts+targeted probes budget4"
```

## Result Snapshot
Persisted `results.tsv` row (`run_id=20260507T200047Z`):
- `f1_score=1.0000`
- `latency_ms=288.92`
- `vram_gb=3.49`
- `status=keep`

Routing telemetry:
- `metadata_shortcut=2`
- `targeted_probe=1`
- `full_mm=2`
- `shortcut_ratio=0.75`

## Interpretation
- This configuration recovered quality sharply versus `EDG-372 r1` (`F1 0.65 -> 1.00`) while remaining far below the 4s latency budget (`288.92 ms`).
- Compared with the forced full-MM micro-check in `EDG-370` (`11465.71 ms`, `F1 0.75`), selective routing with constrained probes materially improves both efficiency and measured quality on this 4-sample slice.
- This is still a micro-check, so promotion confidence needs a slightly larger comparable sample to reduce variance.

## Files Updated
- `results.tsv`
- `docs/superpowers/research_logs/EDG-374_2026-05-07.md`

## Next Action
- Run a follow-up comparable check at `TRIAGE_EVAL_MAX_SAMPLES=8` with the same routing knobs (`metadata shortcuts on`, `targeted probes on`, `budget=4`) to test stability before considering default promotion.
