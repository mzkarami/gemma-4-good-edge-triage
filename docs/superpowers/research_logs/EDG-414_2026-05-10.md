# EDG-414 — Unlabelled dt0 Infra->Affected Boundary Recovery

## Wake Context
- Wake reason: `issue_assigned`
- Issue: `EDG-414 Edge-Triage Optimization Loop`
- Pending/latest comments in wake payload: none (`0/0`), so work continued directly from EDG-413 follow-up direction.

## Objective
Run a narrow full-MM decision-boundary experiment on the persistent `dt0_unlabelled` confusion slice by allowing `infrastructure -> affected` recovery only when casualty lexical evidence is strong and a strict casualty probe confirms.

## Code Changes
File: `triage_sandbox.py`

1. Added new runtime toggle:
   - `TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY` (default `0`)
2. Added state-hash participation for deterministic experiment identity:
   - `compute_state_hash()` now includes `TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY`.
3. Added lexical helper:
   - `has_strong_affected_lexical_evidence(full_mm_output_text, scenario_text)`
   - Requires at least 2 casualty markers.
4. Added narrow recovery logic in `triage_fn` post full-MM parsing:
   - Only on `_unlabelled_` + `disaster_type == "0"`
   - Only when prediction is `infrastructure_and_utility_damage`
   - Requires `TRIAGE_ENABLE_TARGETED_PROBES=1`
   - Requires strong affected lexical evidence
   - Runs `targeted_predicts_affected_strict(image_path)`
   - Promotes to `affected_injured_or_dead_people` only on strict `[yes]`

## Test Coverage
File: `tests/test_triage_sandbox.py`

- Added `test_compute_state_hash_changes_when_unlabelled_dt0_infra_affected_recovery_changes`
- Added `test_has_strong_affected_lexical_evidence_requires_multiple_markers`

Command run:
```bash
.venv/bin/python -m unittest -q tests.test_triage_sandbox
```
Result: `OK` (`66` tests passed).

## Benchmark Run (`@16`)
Command:
```bash
TRIAGE_EVAL_MAX_SAMPLES=16 TRIAGE_FORCE_RERUN=1 TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY=1 \
  .venv/bin/python triage_sandbox.py \
  --description "[latv2] EDG-414 r1: unlabelled dt0 infra->affected strict-probe recovery @16"
```

Persisted `results.tsv` row:
- `run_id=20260510T100127Z`
- `state_hash=5f617a68b0a1b433095b7422d5be2fc889ae4ad12a9d309dbb35b8e55181944b`
- `f1_score=0.9458`
- `latency_ms=222.00`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard` (historical-best F1 gate)

Routing mix snapshot:
- `metadata_shortcut=6`
- `targeted_probe=8`
- `full_mm=6`
- `shortcut_ratio=0.88`

## Outcome
- F1 plateau remained unchanged at `0.9458` (no quality lift).
- Latency improved vs EDG-413 baseline (`224.32 -> 222.00 ms`, `-2.32 ms`) under comparable `@16` conditions.
- The new boundary is safe and reversible (off by default), with deterministic hash isolation and tests.

## Next Action
Run one paired A/B with only this toggle flipped (`0` vs `1`) under identical forced-rerun conditions and capture per-sample `dt0_unlabelled` audit deltas (`TRIAGE_ENABLE_AUDIT=1`) to verify whether the boundary path is functionally active but net-neutral on aggregate F1.
