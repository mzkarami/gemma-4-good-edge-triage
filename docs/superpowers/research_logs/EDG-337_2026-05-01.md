# EDG-337 - Context Floor Micro-Sweep (`n_ctx=931`) (2026-05-01)

## Objective
- Continue post-EDG-336 optimization with a low-risk runtime sweep aimed at reducing latency while preserving frontier F1.
- Hold all promoted knobs constant from EDG-335/EDG-336 except context:
  - `n_gpu_layers=47`
  - `n_batch=192`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=11`

## Experiment
- `EDG-337 r1` (`n_ctx=931`)
  - Command:
    - `TRIAGE_FORCE_RERUN=1 TRIAGE_N_CTX=931 uv run triage_sandbox.py --description "[latv2] EDG-337 r1: n_ctx=931 micro-sweep on gpu47/batch192/probebudget11"`
  - Result:
    - Accuracy: `0.9800`
    - F1: `0.9794`
    - Latency: `160.36 ms`
    - TTFT: `155.62 ms`
    - Peak VRAM: `3.84 GB`
    - State hash: `02d0ae299ae035268661375a53afd8d4165c83b303ce9c68a96b464c6a02e481`
    - Routing mix: `metadata_shortcut=23`, `targeted_probe=19`, `probe_budget_fallback=1`, `full_mm=13`, `shortcut_ratio=0.84`
  - Decision: `keep`

## Assessment
- The sweep preserved frontier F1 (`0.9794`) and improved over recent control repeats (`160.95 ms` from EDG-336 r5).
- It did not beat the all-time best observed latency (`159.78 ms`, EDG-335 r5), so this is a frontier-safe but non-record variant.
- Current production defaults remain unchanged (`n_ctx=933`) pending repeat stability checks around `n_ctx=931`.

## Next Step
- Run a repeat pair at `n_ctx=931` under forced rerun to quantify variance and verify whether `160.36 ms` is stable.
- If repeat median stays <= `160.4 ms` with F1 `0.9794`, consider promoting `n_ctx=931`; otherwise keep `n_ctx=933` as default.
