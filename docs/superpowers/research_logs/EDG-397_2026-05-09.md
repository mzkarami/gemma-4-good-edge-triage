# EDG-397 — dt0 Severe Confirmation Sweep

Date: 2026-05-09
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work proceeded directly with the EDG-392 follow-up sweep.

## Objective
Run the planned 16-sample controlled sweep to test whether additional full-MM coverage recovers F1 without violating latency budget.

## Changes Implemented
File: `triage_sandbox.py`

Added a new routing control:
- `TRIAGE_CONFIRM_DT0_SEVERE_AFFECTED_WITH_FULL_MM=0` (default)

Behavioral effect:
- On `event_bucket=other`, `disaster_type=0`, `_severe_` slices with targeted probe=`yes`:
  - default (`0`): keep prior behavior (direct affected return)
  - enabled (`1`): defer to full-MM for confirmation instead of immediate affected return
- Included in `compute_state_hash()` for reproducible experiment identity.

Validation update:
- Added hash sensitivity test in `tests/test_triage_sandbox.py`:
  - `test_compute_state_hash_changes_when_dt0_severe_confirmation_changes`

## Verification
### Focused unit checks
Executed:
```bash
uv run python -m unittest -q \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_dt0_severe_confirmation_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_probe_budget_changes
```

Result:
- `Ran 2 tests ... OK`

### Controlled sweep (16 samples)

Run A:
```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_ENABLE_METADATA_SHORTCUTS=0 TRIAGE_ENABLE_TARGETED_PROBES=1 \
uv run triage_sandbox.py --description "[latv2] EDG-397 r1: sweep A shortcuts off, probes on"
```

Persisted row:
- `run_id=20260509T080114Z`
- `state_hash=dfaef6d2aa9df419990a9b5ed5155a6f9c25736fe4681c90e8c6c0b34afa563c`
- `f1_score=0.8284`
- `latency_ms=492.94`
- `vram_gb=3.59`
- `total_samples=16`
- `status=discard`
- Routing mix: `metadata_shortcut=0`, `targeted_probe=8`, `full_mm=12`, `shortcut_ratio=0.50`

Run B:
```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_ENABLE_METADATA_SHORTCUTS=1 TRIAGE_ENABLE_TARGETED_PROBES=1 \
TRIAGE_ENABLE_DT0_SEVERE_PROBE=1 TRIAGE_CONFIRM_DT0_SEVERE_AFFECTED_WITH_FULL_MM=1 \
uv run triage_sandbox.py --description "[latv2] EDG-397 r2: sweep B dt0 severe full-mm confirm + shortcuts on"
```

Persisted row:
- `run_id=20260509T080114Z`
- `state_hash=7110dd40daca96c1b9e51a296ec8b537d62da9af2b11b797acd8609e0e17f84c`
- `f1_score=0.8284`
- `latency_ms=505.50`
- `vram_gb=3.60`
- `total_samples=16`
- `status=discard`
- Routing mix: `metadata_shortcut=1`, `targeted_probe=13`, `full_mm=11`, `shortcut_ratio=0.88`

## Interpretation
- Both sweep variants significantly underperformed the recent 16-sample EDG-392 plateau (`F1=0.9458`) and historical-best gate (`1.0000` on prior 16-sample keep).
- Extra full-MM confirmations on `dt0 severe` did not recover quality in this slice under current prompts.
- Latency stayed within edge budget (`< 4s`) but moved up materially vs recent latv2 keeps.

## Next Action
1. Revert to EDG-392 routing defaults for `other` buckets as the safe baseline.
2. Shift next EDG-397 iteration from routing to targeted-prompt quality on the `none_dt6` / `other_dt0` confusion slices:
   - tighten yes/no probe wording with two alternatives,
   - run paired A/B at 16 samples,
   - keep only if F1 exceeds `0.9458` without >10% latency increase over EDG-392 (`223.85 ms`).
