# EDG-415 — A/B Audit of Unlabelled dt0 Infra->Affected Recovery

Date: 2026-05-10
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so heartbeat proceeded directly to the previously queued EDG-414 follow-up A/B.

## Objective
Execute the exact paired comparison requested in EDG-414 next action:
- Control: `TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY=0`
- Treatment: `TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY=1`
With all other knobs fixed, `@16` sample budget, forced rerun, and audit enabled.

## Commands Executed
```bash
TRIAGE_EVAL_MAX_SAMPLES=16 TRIAGE_FORCE_RERUN=1 TRIAGE_ENABLE_AUDIT=1 \
TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY=0 \
.venv/bin/python triage_sandbox.py \
  --description "[latv2] EDG-415 r1a: control unlabelled dt0 infra->affected recovery OFF @16 audit"

TRIAGE_EVAL_MAX_SAMPLES=16 TRIAGE_FORCE_RERUN=1 TRIAGE_ENABLE_AUDIT=1 \
TRIAGE_UNLABELLED_DT0_INFRA_AFFECTED_RECOVERY=1 \
.venv/bin/python triage_sandbox.py \
  --description "[latv2] EDG-415 r1b: treatment unlabelled dt0 infra->affected recovery ON @16 audit"
```

## Results
### Run A (Control, recovery OFF)
- `run_id=20260510T120047Z`
- `state_hash=edae0dfadb70ccb05267ae3e7dfb44aa90817fe8320e2be9cc228e86f0655de7`
- `f1_score=0.9458`
- `latency_ms=220.24`
- `vram_gb=3.56`
- `status=discard`

Audit summary:
- `AUDIT: records=16 mismatches=1`
- `AUDIT_MISMATCH`: `ASONAM2017_8` (`gold=infrastructure_and_utility_damage`, `pred=rescue_volunteering_or_donation_effort`, `route=full_mm`, `dt=0`, `event=ecuador_eq_unlabelled_im_665.jpg`)
- `AUDIT_UNLABELLED_DT0`: `samples=3`, `unique_routes=1`, route distribution: `full_mm=3`

### Run B (Treatment, recovery ON)
- `run_id=20260510T120101Z`
- `state_hash=5f617a68b0a1b433095b7422d5be2fc889ae4ad12a9d309dbb35b8e55181944b`
- `f1_score=0.9458`
- `latency_ms=229.03`
- `vram_gb=3.56`
- `status=discard`

Audit summary:
- `AUDIT: records=16 mismatches=1`
- Same single mismatch on `ASONAM2017_8` with the same predicted class and route (`full_mm`)
- `AUDIT_UNLABELLED_DT0` unchanged (`samples=3`, all `full_mm`)

## Interpretation
- The `infra->affected` recovery path did not materially change any audited `dt0_unlabelled` sample outcomes in this comparable `@16` run.
- Aggregate quality remained on the established plateau (`F1=0.9458`).
- Treatment run was slower (`229.03 ms` vs `220.24 ms`, `+8.79 ms`) with no quality gain.
- Based on observed behavior, the new boundary is currently net-neutral to negative and should remain disabled by default.

## Durable Artifacts
- `results.tsv` appended with two EDG-415 rows (`r1a` control and `r1b` treatment).
- Runtime logs:
  - `run_edg415_r1a.log`
  - `run_edg415_r1b.log`

## Next Action
Narrow the follow-up to the single persistent failing slice (`ASONAM2017_8`, `ecuador_eq_unlabelled_im_665.jpg`) by adding targeted, temporary audit instrumentation that records whether:
1. strong affected lexical evidence was detected,
2. strict probe was invoked,
3. strict probe returned `[yes]/[no]`,
before class remap. Then run a 1-sample deterministic replay to confirm whether the recovery gate is inactive due to lexical gating or probe outcome.
