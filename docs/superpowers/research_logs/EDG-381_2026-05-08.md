# EDG-381 - Edge-Triage Optimization Loop (2026-05-08)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work proceeded from the explicit next action in `EDG-376`.

## Objective
- Run the 16-sample comparable check for the EDG-374/EDG-376 routing bundle:
  - `TRIAGE_ENABLE_METADATA_SHORTCUTS=1`
  - `TRIAGE_ENABLE_TARGETED_PROBES=1`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4`
  - `TRIAGE_EVAL_MAX_SAMPLES=16`
- Validate whether frontier-quality F1 holds beyond the earlier 4/8-sample checks while staying below 4s latency.

## Work Completed
Executed:

```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_ENABLE_METADATA_SHORTCUTS=1 TRIAGE_ENABLE_TARGETED_PROBES=1 \
TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4 \
uv run triage_sandbox.py \
  --description "[latv2] EDG-381 r1: 16-sample comparable check metadata shortcuts+targeted probes budget4"
```

## Result Snapshot
Persisted `results.tsv` row (`run_id=20260508T080052Z`):
- `f1_score=0.9458`
- `latency_ms=206.48`
- `vram_gb=3.56`
- `total_samples=16`
- `status=discard`
- status note: `discarded: F1 below historical best (0.9458 vs 1.0000, epsilon=0.0001)`

Routing telemetry:
- `metadata_shortcut=7`
- `targeted_probe=8`
- `probe_budget_fallback=0`
- `full_mm=5`
- `shortcut_ratio=0.94`

## Interpretation
- Latency remains strong (`206.48 ms`, safely below the `<4s` target), but quality regressed at this larger sample (`F1=0.9458`), so the EDG-374/376 bundle should not be promoted as a default.
- The higher shortcut ratio on this run suggests over-aggressive shortcut/probe routing on some examples in the 16-sample slice.

## Files Updated
- `results.tsv`
- `docs/superpowers/research_logs/EDG-381_2026-05-08.md`

## Next Action
- Run an ablation to isolate the regression source while keeping `TRIAGE_EVAL_MAX_SAMPLES=16` fixed:
  1. Keep metadata shortcuts on, disable targeted probes (`TRIAGE_ENABLE_TARGETED_PROBES=0`).
  2. Keep targeted probes on, disable metadata shortcuts (`TRIAGE_ENABLE_METADATA_SHORTCUTS=0`).
- Compare F1 deltas against this EDG-381 baseline and retain the lowest-latency variant that restores frontier-quality F1.
