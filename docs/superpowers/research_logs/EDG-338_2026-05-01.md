# EDG-338 - `n_ctx=931` Repeat Stability Check (2026-05-01)

## Objective
- Validate whether the EDG-337 micro-win (`n_ctx=931`, F1 `0.9794`, latency `160.36 ms`) is stable enough to promote over the current default (`n_ctx=933`).
- Hold all other promoted knobs constant:
  - `n_gpu_layers=47`
  - `n_batch=192`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=11`

## Runs
- `EDG-338 r1` and `r2`:
  - F1: `0.9794`
  - Latency: `266.82 ms`, `266.81 ms`
  - Note: these two were launched concurrently and are not valid for clean latency comparison due to GPU contention.

- `EDG-338 r3` and `r4`:
  - F1: `0.9794`
  - Latency: `266.91 ms`, `266.92 ms`
  - Note: also affected by overlap/contended execution and treated as invalid timing samples.

- `EDG-338 r5` and `r6` (isolated sequential controls):
  - F1: `0.9794`
  - Latency: `163.13 ms`, `163.81 ms`
  - TTFT: `158.13 ms`, `158.62 ms`
  - Peak VRAM: `3.83-3.84 GB`
  - State hash: `02d0ae299ae035268661375a53afd8d4165c83b303ce9c68a96b464c6a02e481`

## Decision
- `discard` for promotion.
- Rationale: isolated repeats preserve frontier F1 but do not beat the best-known latency (`159.78 ms`, EDG-335 r5) and are slower than the current default baseline band.

## Current Defaults
- Keep runtime defaults unchanged in `triage_sandbox.py`:
  - `N_CTX=933`
  - `N_GPU_LAYERS=47`
  - `LLAMA_N_BATCH=192`
  - probe budget `11`

## Next Step
- Shift exploration away from context floor sweeps and toward low-risk routing/accounting overhead trims that do not alter targeted-probe prompt semantics.

## Follow-up Micro-Optimization (r7)
- `EDG-338 r7` (compiled keyword-regex cache in `contains_keyword`)
  - Command:
    - `TRIAGE_FORCE_RERUN=1 uv run triage_sandbox.py --description "[latv2] EDG-338 r7: cache compiled keyword regex in contains_keyword (default nctx933/gpu47/batch192/budget11)"`
  - Result:
    - F1: `0.9794`
    - Latency: `166.59 ms`
    - TTFT: `162.09 ms`
    - VRAM: `3.84 GB`
  - Decision: `discard`

### r7 Conclusion
- The regex-cache change did not produce measurable routing-speed benefit in end-to-end latency and regressed relative to the current frontier band.
- Change rolled back immediately; production defaults and code path remain unchanged.
