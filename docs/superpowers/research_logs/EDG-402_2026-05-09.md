# EDG-402 — dt0 Severe Full-MM Stabilization Calibration

Date: 2026-05-09
Agent: Engineer (Researcher)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work proceeded immediately.

## Objective
Apply a narrow decoder-side stabilization on known dt0 severe confusion without broad routing changes, then run a comparable 16-sample audit check.

## Code Changes
Files updated:
- `triage_sandbox.py`
- `tests/test_triage_sandbox.py`

### New behavior
- Added runtime knob (default ON):
  - `TRIAGE_CONFIRM_DT0_SEVERE_AFFECTED_WITH_STRICT_PROBE=0|1`
- Added helper:
  - `should_confirm_dt0_severe_affected_with_strict_probe(...)`
- Added post-full-MM calibration:
  - On `dt0` severe metadata slice, if full-MM predicts `affected_injured_or_dead_people`, run strict affected probe confirmation.
  - If strict confirmation is negative, demote to `infrastructure_and_utility_damage`.
- Added knob to `compute_state_hash()` to preserve experiment reproducibility.

## Verification
Focused unit checks:
```bash
uv run python -m unittest -q \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_dt0_severe_strict_probe_confirmation_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_should_confirm_dt0_severe_affected_with_strict_probe \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_dt0_severe_confirmation_changes
```
Result: `Ran 3 tests ... OK`

## Experiment
Executed:
```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_ENABLE_AUDIT=1 TRIAGE_EVAL_MAX_SAMPLES=16 \
TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=baseline TRIAGE_ENABLE_DT0_SEVERE_PROBE=0 \
uv run triage_sandbox.py --description "[latv2] EDG-402 r1: dt0 severe full-mm strict confirmation calibration @16"
```

### Results
- `run_id=20260509T160705Z`
- `state_hash=780f14db18500327921bf58d70869d05973f68f726fff28635c29eac50ddcd33`
- `f1_score=0.9458`
- `latency_ms=221.57`
- `vram_gb=3.56`
- `status=discard` (below historical best gate)

Audit summary:
- `mismatches=1`
- Remaining mismatch unchanged from baseline:
  - `idx=8`, `event=ecuador_eq_unlabelled_im_665.jpg`, `gold=infrastructure_and_utility_damage`, `pred=rescue_volunteering_or_donation_effort`, `route=full_mm`

## Interpretation
- The severe-dt0 false-affected guard is now in place and reproducible, but this 16-sample control was already dominated by an unlabelled-dt0 rescue-vs-infrastructure miss.
- Net quality stayed flat (`0.9458`) with slight latency improvement vs EDG-400 baseline control (`224.75 -> 221.57 ms`).

## Next Action
1. Target the residual `dt0_unlabelled` full-MM rescue-vs-infrastructure confusion with a narrowly gated confirmation policy.
2. Keep the new severe-dt0 strict confirmation guard in place as a low-risk stabilization layer.
