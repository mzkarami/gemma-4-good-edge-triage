# EDG-333 Heartbeat Log (2026-05-01)

## Scope
- Issue: `EDG-333 Edge-Triage Optimization Loop`
- Focus this run: promote the repeated `n_ctx=933` candidate to default and validate on a fresh forced benchmark.

## Change Applied
- Updated `triage_sandbox.py` defaults:
  - `TRIAGE_N_CTX`: `934 -> 933`
  - `EXPERIMENT_SIGNATURE`: updated to `edg333-r1-...-nctx933-...`

## Run Executed
- Command:
  - `TRIAGE_FORCE_RERUN=1 uv run triage_sandbox.py --description "EDG-333 r1: promote default n_ctx to 933 from EDG-332 repeat stability"`
- Result row (`results.tsv`):
  - Timestamp: `20260501T080125Z`
  - State hash: `d74b4150cc5f42c3396bfaeb4602fa0065e76ecf7e97b4d10ce16e92b49bf410`
  - F1: `0.9794`
  - Latency: `164.74 ms`
  - Peak VRAM: `3.84 GB`
  - Status: `discard`
  - Decision note: same-F1 band, slower than keep baseline by `4.00 ms`.

## Interpretation
- Promoted `n_ctx=933` keeps frontier F1 and stable VRAM but this specific forced run landed on the slower side of the known variance band.
- Existing best keep baseline remains `160.74 ms` (EDG-329 budget=11), so this default-only promotion is not a latency improvement by current gating rules.

## Next Action
- Run a controlled repeat pair under `EDG-333` with the same promoted default to quantify variance for this new state hash.
- If repeats remain above `~163.7 ms`, pivot to a narrow `n_batch` micro-sweep (`176/184/192`) while holding `n_ctx=933` and probe-budget=11 fixed.
