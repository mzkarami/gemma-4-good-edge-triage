# EDG-436 — other_dt0 Metadata Shortcut Strict-Confirmation Probe

Date: 2026-05-11
Agent: Engineer (Researcher)

## Wake Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`)
- Action taken: implemented and benchmarked a narrow strict-confirmation gate for `other_dt0` metadata shortcut routing.

## Objective
Reduce false negatives from `metadata_shortcut_other_dt0` (notably `ASONAM2017_38`) by requiring a strict affected probe and deferring to full-MM confirmation when strict evidence indicates likely human harm.

## Code Changes
Files:
- `triage_sandbox.py`
- `tests/test_triage_sandbox.py`

### `triage_sandbox.py`
- Added runtime flag:
  - `TRIAGE_CONFIRM_OTHER_DT0_METADATA_SHORTCUT_WITH_STRICT_PROBE` (default `0`)
- Added the new flag to `compute_state_hash()`.
- Updated `event_bucket=other` + `disaster_type=0` metadata-shortcut branch:
  - when the flag is enabled, run `targeted_predicts_affected_strict(image_path)` before returning infra shortcut.
  - if strict probe returns `yes`, skip metadata shortcut and fall through to full-MM confirmation.
  - if strict probe returns `no`, preserve existing `metadata_shortcut_other_dt0` behavior.

### `tests/test_triage_sandbox.py`
Added hash regression test:
- `test_compute_state_hash_changes_when_other_dt0_metadata_strict_confirmation_changes`

## Verification
Command:
```bash
.venv/bin/python -m unittest -q \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_none_dt5_probe_no_strict_confirmation_changes \
  tests.test_triage_sandbox.TestTriageSandbox.test_compute_state_hash_changes_when_other_dt0_metadata_strict_confirmation_changes
```
Result: `Ran 2 tests ... OK`

## Benchmark Run (r1)
Command toggles:
- `TRIAGE_EVAL_MAX_SAMPLES=64` (effective 50)
- `TRIAGE_FORCE_RERUN=1`
- `TRIAGE_ENABLE_AUDIT=1`
- `TRIAGE_AUDIT_TRACE_UNLABELLED_DT0=1`
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_STRICT_PROBE=1`
- `TRIAGE_UNLABELLED_DT0_RESCUE_INFRA_FALLBACK_GATE=1`
- `TRIAGE_UNLABELLED_DT0_RESCUE_INFRA_FALLBACK_POLICY=infra_only`
- `TRIAGE_CONFIRM_UNLABELLED_DT0_RESCUE_WITH_INFRA_TIEBREAK=1`
- `TRIAGE_CONFIRM_OTHER_DT0_METADATA_SHORTCUT_WITH_STRICT_PROBE=1`

Run artifact:
- `run_edg436_r1.log`

Persisted ledger row:
- `run_id=20260511T180152Z`
- `state_hash=3b372dfbe6530daad8e36e360dce8195b6dfe2667c77a13505ca24dcb7228628`
- `f1=0.9374`
- `latency_ms=250.90`
- `ttft_ms=222.81`
- `vram_gb=3.77`
- `status=discard` (below historical best F1)

Audit mismatches (unchanged count=3):
- `ASONAM2017_20`: gold `affected_injured_or_dead_people` -> pred `not_humanitarian` (`targeted_probe_none_dt5_no`)
- `ASONAM2017_38`: gold `affected_injured_or_dead_people` -> pred `infrastructure_and_utility_damage` (`metadata_shortcut_other_dt0`)
- `ASONAM2017_44`: gold `not_humanitarian` -> pred `infrastructure_and_utility_damage` (`full_mm`)

## Interpretation
- The new strict-confirmation gate did not recover `ASONAM2017_38` on this slice; strict probe path did not convert that sample into an affected/full-MM correction.
- Latency regressed materially vs EDG-432 baseline (`185.64 -> 250.90 ms`) without quality gain.
- Edge constraints remain satisfied (`<4s`, `<4GB`) but this knob is not promotable.

## Next Action
1. Add focused audit telemetry for `other_dt0` strict-probe outcomes (`strict_probe_invoked/yes`) to verify whether `ASONAM2017_38` is failing due to strict-probe `no` vs full-MM post-confirmation collapse.
2. Run a single targeted ablation at `@64` with `TRIAGE_ESCALATE_OTHER_DT0_TO_FULL_MM=1` and strict-probe gate disabled to isolate whether full-MM alone can recover `ASONAM2017_38` acceptably.
