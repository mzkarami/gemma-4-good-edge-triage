# Logs Workspace

Generated benchmark logs belong here, for example:

```bash
mkdir -p logs
uv run triage_sandbox.py --description "local experiment" > logs/run.log 2>&1
```

The public repo keeps only this README and `.gitkeep`. Do not commit generated `*.log` files, local stack logs, uploaded image traces, or field reports.
