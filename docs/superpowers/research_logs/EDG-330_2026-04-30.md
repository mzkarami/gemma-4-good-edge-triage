# EDG-330 Heartbeat Log (2026-04-30)

## Scope
- Issue: `EDG-330 Edge-Triage Optimization Loop`
- Focus this run: validate the in-flight optimization package around probe-budget control, stronger state hashing, and guarded CPU fallback handling.

## Baseline Context
- Current best kept row before this run:
  - Timestamp: `20260430T160323Z`
  - F1: `0.9794`
  - Latency: `160.74 ms`
  - VRAM: `4.11 GB`
  - Description: `EDG-329 sweep2: cap targeted probes at 11 per bucket`

## Run Executed
- Command:
  - `uv run triage_sandbox.py --description "EDG-330 r1 probe-budget11 hash-hardening cpu-guard-cap"`
- Result row (`results.tsv`):
  - Timestamp: `20260430T180101Z`
  - State hash: `2de7702422d3effe8fbe1a6c38a274b18b2cccae9da5d6a37df5360f335684b4`
  - F1: `0.9794`
  - Latency: `170.91 ms`
  - Peak VRAM: `10.16 GB`
  - Status: `discard`
  - Decision note: same-F1 band, slower than keep baseline by `10.17 ms`.

## Routing Mix Snapshot
- `text_fast_path=0`
- `metadata_shortcut=23`
- `targeted_probe=19`
- `probe_budget_fallback=1`
- `full_mm=13`
- `shortcut_ratio=0.84`

## Interpretation
- Accuracy/F1 stayed at the current frontier (`0.9794`).
- Latency regressed against the current best keep baseline, so this exact runtime path remains non-promotable.
- Probe-budget fallback triggered once, indicating the cap logic is active and observable in real traffic.

## Next Action
- Keep `probe-budget=11` as the active candidate baseline.
- Next sweep should isolate the latency regression source under identical guard conditions (especially full-MM load share and probe-trigger order) before any further prompt changes.

## Continuation (Same Heartbeat)

### Variance Isolation Pass
- Initial repeat attempt (`EDG-330 r2`) was dedupe-skipped due identical state hash.
- For true repeat measurements, reruns must set `TRIAGE_FORCE_RERUN=1`.

### Forced Repeat Results (`probe-budget=11`, unchanged config)
- `EDG-330 r3 forced-repeat1 budget11 variance-check`
  - F1: `0.9794`
  - Latency: `162.02 ms`
  - Peak VRAM: `10.16 GB`
  - Routing mix: `text_fast_path=0, metadata_shortcut=23, targeted_probe=19, probe_budget_fallback=1, full_mm=13, shortcut_ratio=0.84`
- `EDG-330 r3 forced-repeat2 budget11 variance-check`
  - F1: `0.9794`
  - Latency: `161.69 ms`
  - Peak VRAM: `10.16 GB`
  - Routing mix: `text_fast_path=0, metadata_shortcut=23, targeted_probe=19, probe_budget_fallback=1, full_mm=13, shortcut_ratio=0.84`

### Revised Interpretation
- The earlier `170.91 ms` result is likely a transient runtime outlier, not a stable regression.
- Under forced reruns, latency clusters near `~162 ms`, which is within `~1.3 ms` of the current best keep baseline (`160.74 ms`) and inside the `3.00 ms` epsilon band.
- Configuration remains promotable from a latency/F1 perspective; no prompt/routing logic rollback is indicated.
