# EDG-366 - Edge-Triage Optimization Loop (2026-05-07)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments: none in wake payload (`0/0`), so action proceeded directly from prior EDG-365 next-step guidance.

## Objective
- Execute the EDG-365 follow-up benchmark for ledger hot-path savings.
- Ensure cached-row migration does not introduce runtime regressions.

## Work Completed
1. Ran a controlled micro-benchmark of the pre-model-load ledger-read bundle using current `results.tsv`.
2. Found and fixed a runtime incompatibility in `load_best_latency_for_f1_floor`:
   - `_load_results_rows` now returns parsed cell arrays.
   - `load_best_latency_for_f1_floor` still attempted `row.split("\t")`, causing `AttributeError` during warm/cold benchmark.
   - Updated loop to iterate parsed `cells` directly.

## Validation
### Targeted tests
```bash
uv run python -m unittest \
  tests.test_triage_sandbox.TestTriageSandbox.test_results_rows_cache_invalidates_after_append \
  tests.test_triage_sandbox.TestTriageSandbox.test_load_best_latency_for_f1_floor_migrates_legacy_schema \
  tests.test_triage_sandbox.TestTriageSandbox.test_run_triage_does_not_duplicate_latency_tag_in_description
```
- Result: `Ran 3 tests` / `OK`

### Controlled benchmark (current workspace `results.tsv`)
Benchmark bundle:
- `load_recorded_state_hashes`
- `load_best_recorded_f1`
- `load_best_latency_for_f1_floor`
- `has_blocked_reason_for_state_hash`
- `load_recent_keep_latencies_for_state_hash`

Method:
- Cold: invalidate row cache before each sample (60 samples).
- Warm: seed cache once, then sample repeated reads (300 samples).

Observed:
- Rows parsed: `611`
- Cold median: `0.8157 ms` (p95 `0.8517 ms`)
- Warm median: `0.5751 ms` (p95 `0.5826 ms`)
- Median speedup: `1.42x`

## Files Updated
- `triage_sandbox.py`
- `docs/superpowers/research_logs/EDG-366_2026-05-07.md`

## Next Action
- Add a focused regression test that would have caught parsed-row/string-row mismatches across all results readers, then run a narrow end-to-end `run_triage` smoke invocation with `TRIAGE_FORCE_RERUN=1` to confirm no pre-load guard regressions.
