# EDG-331 Heartbeat Log (2026-04-30)

## Scope
- Issue: `EDG-331 Edge-Triage Optimization Loop`
- Focus this run: fix VRAM telemetry drift under shared GPU usage and re-validate baseline metrics.

## Change Applied
- Updated `triage_sandbox.py` VRAM accounting to prefer process-scoped GPU memory via:
  - `nvidia-smi --query-compute-apps=pid,used_memory`
- Kept existing fallbacks:
  - torch CUDA peak allocation
  - device-level `nvidia-smi memory.used` as last resort
- Added parser tests for process VRAM extraction in `tests/test_triage_sandbox.py`.

## Run Executed
- Command:
  - `TRIAGE_FORCE_RERUN=1 uv run triage_sandbox.py --description "EDG-331 r1: process-scoped VRAM accounting for shared GPU hosts"`
- Result row (`results.tsv`):
  - Timestamp: `20260430T200206Z`
  - State hash: `2de7702422d3effe8fbe1a6c38a274b18b2cccae9da5d6a37df5360f335684b4`
  - F1: `0.9794`
  - Latency: `169.04 ms`
  - Peak VRAM: `3.84 GB`
  - Status: `discard`
  - Decision note: same-F1 band, slower than keep baseline by `8.30 ms`.

## Interpretation
- VRAM now aligns with historical edge-compatible levels (`~3.8-4.1 GB`) rather than inflated `~10.16 GB` values.
- The telemetry fix appears valid and improves comparability for edge-memory constraints.
- Accuracy remained at the frontier (`0.9794`), but this single run is latency-worse than best keep (`160.74 ms`), so no promote on latency.

## Operational Note
- `pytest` is not installed in the current `uv` environment, so added tests were not executed in this run.

## Next Action
- Keep probe-budget=11 routing logic unchanged for now.
- Run a forced repeat pair under EDG-331 to isolate latency variance now that VRAM telemetry is corrected.

## Continuation (Same Heartbeat)

### Forced Repeat Results (unchanged config)
- `EDG-331 r2 forced-repeat1 process-vram variance-check`
  - F1: `0.9794`
  - Latency: `163.29 ms`
  - Peak VRAM: `3.84 GB`
  - Routing mix: `text_fast_path=0, metadata_shortcut=23, targeted_probe=19, probe_budget_fallback=1, full_mm=13, shortcut_ratio=0.84`
- `EDG-331 r3 forced-repeat2 process-vram variance-check`
  - F1: `0.9794`
  - Latency: `163.80 ms`
  - Peak VRAM: `3.84 GB`
  - Routing mix: `text_fast_path=0, metadata_shortcut=23, targeted_probe=19, probe_budget_fallback=1, full_mm=13, shortcut_ratio=0.84`

### Revised Interpretation
- Corrected VRAM accounting is stable across repeated runs (`3.84 GB`).
- Latency under repeats (`163.29-163.80 ms`) is near the current best keep baseline (`160.74 ms`) and within the `3.00 ms` non-improving epsilon band.
- The earlier `169.04 ms` appears to be runtime variance rather than persistent regression.

### Decision
- Keep the instrumentation change for process-scoped VRAM telemetry.
- Continue EDG-331 with performance-focused sweeps from this corrected measurement baseline.

### Follow-up Sweep
- `EDG-331 r4 sweep: n_ctx=933 on process-vram baseline`
  - State hash: `02c2d8bdce58a7b451b1427dcde64738e3fd8cc19dd34b6f29fbf576114d9800`
  - F1: `0.9794`
  - Latency: `163.15 ms`
  - Peak VRAM: `3.84 GB`
  - Routing mix: `text_fast_path=0, metadata_shortcut=23, targeted_probe=19, probe_budget_fallback=1, full_mm=13, shortcut_ratio=0.84`

### Updated Direction
- `n_ctx=933` is currently the fastest EDG-331 variant under corrected process-scoped VRAM accounting while preserving frontier F1.
- Continue subsequent sweeps from `n_ctx=933` rather than `934` if the next run confirms stability.
