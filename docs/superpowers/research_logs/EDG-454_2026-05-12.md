# EDG-454 — Strict Main Prompt + Targeted Probe Prompt v3 Ablation

Date: 2026-05-12
Agent: Engineer (Researcher)

## Wake Handling
- Wake reason: `issue_assigned`
- Pending/latest comments in wake payload: none (`0/0`)
- Action taken: executed the EDG-452 next action by evaluating `TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=v3` under the strict severe-`dt0` main prompt.

## Objective
Test whether targeted probe wording `v3` improves residual severe `rescue` false positives (`ASONAM2017_34`, `ASONAM2017_38`) while preserving the EDG-452 F1 gain and latency profile.

## Controlled Ablation Run
Command:
```bash
TRIAGE_FORCE_RERUN=1 \
TRIAGE_EVAL_MAX_SAMPLES=64 \
TRIAGE_ENABLE_AUDIT=1 \
TRIAGE_AUDIT_TRACE_OTHER_DT0=1 \
TRIAGE_CONFIRM_OTHER_DT0_METADATA_SHORTCUT_WITH_STRICT_PROBE=0 \
TRIAGE_ESCALATE_OTHER_DT0_TO_FULL_MM=1 \
TRIAGE_OTHER_DT0_ESCALATION_PRIORITY_IMAGE_IDS=asonam2017_38 \
TRIAGE_MAIN_PROMPT_VARIANT=severe_dt0_rescue_guard_strict \
TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=v3 \
.venv/bin/python triage_sandbox.py --description "[latv2] EDG-454 r1: strict severe dt0 + targeted probe prompt v3" \
| tee run_edg454_r1.log
```

Persisted metrics:
- Accuracy: `0.8600`
- F1: `0.8712`
- Latency: `275.93 ms`
- TTFT: `258.91 ms`
- Peak VRAM: `3.74 GB`
- State hash: `dd73641d38e10c1c3a3c5c4ef96e60303600398d7e7d6b428856f5a2a0e29dba`

## Comparison vs EDG-452 r1 (current frontier strict prompt)
- F1 `0.9233 -> 0.8712` (`-0.0521`)
- Latency `250.41 -> 275.93 ms` (`+25.52 ms`)

## Observations
- The `v3` targeted probe wording significantly increased targeted-probe activity (`targeted_probe=19`) relative to the strict-prompt-only frontier behavior and degraded both quality and latency.
- Severe `dt0` rescue confusions persisted:
  - `ASONAM2017_34`: gold `infrastructure_and_utility_damage`, pred `rescue_volunteering_or_donation_effort` (`full_mm`)
  - `ASONAM2017_38`: gold `affected_injured_or_dead_people`, pred `rescue_volunteering_or_donation_effort` (`full_mm`)
- Additional regressions appeared in non-severe slices (`ASONAM2017_6`, `ASONAM2017_48`), indicating collateral damage from stricter probe phrasing.

## Conclusion
`TRIAGE_TARGETED_PROBE_PROMPT_VARIANT=v3` is a regression under the strict severe-`dt0` main prompt and should not be promoted.

## Next Action
Keep `TRIAGE_MAIN_PROMPT_VARIANT=severe_dt0_rescue_guard_strict` as the active frontier and run a focused `full_mm` rescue correction ablation for severe `dt0` routes only (e.g., severe-route conditional rescue demotion gate requiring explicit casualty or extraction cues post-`full_mm`) to target the remaining hard errors without broad probe-side regressions.
