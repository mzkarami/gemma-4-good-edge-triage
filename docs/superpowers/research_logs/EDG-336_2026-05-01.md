# EDG-336 - Frontier Stability Sweep Around `gpu47/n_ctx933` (2026-05-01)

## Objective
- Continue the optimization loop from EDG-335 by probing nearby runtime knobs for additional latency gains without dropping frontier F1.
- Baseline target to beat:
  - F1: `0.9794`
  - Latency: `159.78 ms`
  - Reference state: EDG-335 r5 (`n_ctx=933`, `n_batch=192`, `n_gpu_layers=47`, probe-budget `11`).

## Experiment Matrix
- `EDG-336 r1` (`n_ctx=932`)
  - Command:
    - `TRIAGE_N_CTX=932 uv run triage_sandbox.py --description "[latv2] EDG-336 r1: n_ctx=932 micro-sweep on gpu47/batch192/probebudget11"`
  - Result:
    - F1: `0.9794`
    - Latency: `163.04 ms`
    - VRAM: `3.84 GB`
  - Decision: `discard` (same-F1 band, slower by `+3.26 ms` vs best).

- `EDG-336 r2` (probe budget `10`)
  - Command:
    - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=10 uv run triage_sandbox.py --description "[latv2] EDG-336 r2: probe budget 10 sweep on gpu47/nctx933/batch192"`
  - Result:
    - F1: `0.9572`
    - Latency: `156.24 ms`
    - VRAM: `3.83 GB`
  - Decision: `discard` (hard F1 regression, despite latency gain).

- `EDG-336 r3` (`n_gpu_layers=48`)
  - Command:
    - `TRIAGE_N_GPU_LAYERS=48 uv run triage_sandbox.py --description "[latv2] EDG-336 r3: n_gpu_layers=48 micro-sweep on nctx933/batch192/probebudget11"`
  - Result:
    - F1: `0.9794`
    - Latency: `162.27 ms`
    - VRAM: `3.83 GB`
  - Decision: `discard` (same-F1 band, slower by `+2.49 ms` vs best).

## Conclusions
- Frontier remains unchanged: EDG-335 r5 is still the best latency/F1 operating point.
- Lowering probe budget from `11` to `10` remains unacceptable due to recurrent F1 collapse.
- Nearby context/GPU-layer micro-adjustments (`n_ctx=932`, `n_gpu_layers=48`) do not improve latency at fixed F1.

## Next Step
- Keep defaults unchanged in `triage_sandbox.py` (`n_ctx=933`, `n_batch=192`, `n_gpu_layers=47`, probe-budget `11`).
- Next exploration should target low-risk routing overhead reductions that preserve probe budget `11` (for example, targeted prompt token trimming with forced rerun repeats).

## Continuation Sweep
- `EDG-336 r4` (targeted prompt token trimming)
  - Command:
    - `TRIAGE_FORCE_RERUN=1 uv run triage_sandbox.py --description "[latv2] EDG-336 r4: short targeted-probe prompts v1 on nctx933/gpu47/batch192"`
  - Result:
    - F1: `0.9238`
    - Latency: `160.77 ms`
    - VRAM: `3.84 GB`
    - State hash: `29bd22c5a19c8155767bec14b6244263f71c722294738565df0efe0679850e3f`
  - Decision: `discard` (severe F1 regression vs frontier `0.9794`).

## Updated Conclusion
- The targeted-probe prompt wording is highly sensitivity-coupled to classification quality.
- Aggressive prompt shortening should be treated as high risk despite similar latency.
- Keep production prompt text and defaults unchanged from EDG-335 promoted state.

- `EDG-336 r5` (post-rollback control repeat)
  - Command:
    - `TRIAGE_FORCE_RERUN=1 uv run triage_sandbox.py --description "[latv2] EDG-336 r5: baseline repeat after r4 rollback (nctx933/gpu47/batch192/budget11)"`
  - Result:
    - F1: `0.9794`
    - Latency: `160.95 ms`
    - VRAM: `3.84 GB`
    - State hash: `e7ec3c25ee798e8fb20d93df5dd6edb2d8fdde3ab8d5e7de640a59d7625b33e1`
  - Decision: `keep` (stability confirmation; not a new latency best).
