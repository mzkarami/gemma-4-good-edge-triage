# EDG-372 - Edge-Triage Optimization Loop (2026-05-07)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so execution proceeded with the next actionable step from `EDG-370`.

## Objective
- Run the planned second 4-sample comparable micro-check by re-enabling metadata shortcuts while keeping targeted probes disabled.
- Quantify latency recovery from routing alone and check whether quality remains acceptable.

## Work Completed
Executed:

```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=4 \
TRIAGE_ENABLE_METADATA_SHORTCUTS=1 TRIAGE_ENABLE_TARGETED_PROBES=0 \
uv run triage_sandbox.py \
  --description "[latv2] EDG-372 r1: 4-sample micro-check metadata shortcuts on, targeted probes off"
```

## Result Snapshot
Persisted `results.tsv` row (`run_id=20260507T180055Z`):
- `f1_score=0.6500`
- `latency_ms=176.64`
- `vram_gb=3.46`
- `status=keep`

Routing telemetry:
- `metadata_shortcut=3`
- `targeted_probe=0`
- `full_mm=1`
- `shortcut_ratio=0.75`

## Interpretation
- Latency recovered dramatically versus the forced-full-MM baseline in `EDG-370` (`11465.71 ms -> 176.64 ms` on the same 4-sample micro-check size).
- Quality regressed (`F1 0.75 -> 0.65`), indicating shortcut-only routing (without targeted probes) is currently too aggressive for this slice.
- This run establishes a fast-latency anchor with clear headroom to recover F1 by selectively reintroducing precision checks.

## Files Updated
- `results.tsv`
- `docs/superpowers/research_logs/EDG-372_2026-05-07.md`

## Next Action
- Run a third 4-sample micro-check with metadata shortcuts still enabled and targeted probes enabled at a lower budget:
  - `TRIAGE_ENABLE_METADATA_SHORTCUTS=1`
  - `TRIAGE_ENABLE_TARGETED_PROBES=1`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4`
- Goal: recover F1 relative to `EDG-372 r1` while preserving sub-4s latency and avoiding full-MM regression.
