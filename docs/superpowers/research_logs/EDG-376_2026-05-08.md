# EDG-376 - Edge-Triage Optimization Loop (2026-05-08)

## Wake Delta Handling
- Wake reason: `issue_assigned`
- Pending/latest issue comments in wake payload: none (`0/0`), so work proceeded from the explicit open next action in `EDG-374`.

## Objective
- Execute the follow-up comparable check at `TRIAGE_EVAL_MAX_SAMPLES=8` using:
  - `TRIAGE_ENABLE_METADATA_SHORTCUTS=1`
  - `TRIAGE_ENABLE_TARGETED_PROBES=1`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4`
- Validate whether the recovered quality from EDG-374 holds on a slightly larger sample while staying below 4s latency.

## Work Completed
Executed:

```bash
TRIAGE_FORCE_RERUN=1 TRIAGE_EVAL_MAX_SAMPLES=8 \
TRIAGE_ENABLE_METADATA_SHORTCUTS=1 TRIAGE_ENABLE_TARGETED_PROBES=1 \
TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4 \
uv run triage_sandbox.py \
  --description "[latv2] EDG-376 r1: 8-sample micro-check metadata shortcuts+targeted probes budget4"
```

Also validated the current `EDG-376` code path with targeted tests:

```bash
uv run python -m unittest -q tests.test_triage_sandbox
```

## Result Snapshot
Persisted `results.tsv` row (`run_id=20260507T220106Z`):
- `f1_score=1.0000`
- `latency_ms=281.58`
- `vram_gb=3.52`
- `total_samples=8`
- `status=keep`

Routing telemetry:
- `metadata_shortcut=3`
- `targeted_probe=4`
- `probe_budget_fallback=0`
- `full_mm=4`
- `shortcut_ratio=0.88`

## Interpretation
- The EDG-374 recovery holds on the 8-sample follow-up (`F1=1.00` again) with slightly improved latency (`288.92 -> 281.58 ms`).
- Latency remains comfortably under the `<4s` edge target.
- Probe budget 4 did not force any fallback on this slice (`probe_budget_fallback=0`), so targeted probes remained selective without visible routing saturation.

## Files Updated
- `results.tsv`
- `docs/superpowers/research_logs/EDG-376_2026-05-08.md`

## Next Action
- Run a 16-sample comparable check with the same knobs to reduce small-sample variance before default promotion:
  - `TRIAGE_EVAL_MAX_SAMPLES=16`
  - `TRIAGE_ENABLE_METADATA_SHORTCUTS=1`
  - `TRIAGE_ENABLE_TARGETED_PROBES=1`
  - `TRIAGE_TARGETED_PROBE_BUCKET_BUDGET=4`
- If F1 remains at frontier quality and latency stays stable (<4s), promote this routing bundle as the default candidate.
